<section class="elsewhere" aria-labelledby="elsewhere-heading" data-reveal>
  <span class="sectionNumber" aria-hidden="true">02</span>
  <span class="wordmark" aria-hidden="true">opp</span>
  <h2 id="elsewhere-heading" class="label">联系 / Elsewhere</h2>

  <div class="socials">
    <a class="social" href="https://github.com/oppenheimor" target="_blank" rel="noreferrer">
      <span class="icon">
        <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 .3a12 12 0 0 0-3.8 23.4c.6.1.8-.3.8-.6v-2.1c-3.3.7-4-1.6-4-1.6-.6-1.4-1.4-1.8-1.4-1.8-1.1-.7.1-.7.1-.7 1.2.1 1.8 1.2 1.8 1.2 1.1 1.8 2.8 1.3 3.5 1 .1-.8.4-1.3.8-1.6-2.7-.3-5.5-1.3-5.5-5.9 0-1.3.5-2.4 1.2-3.2-.1-.3-.5-1.5.1-3.2 0 0 1-.3 3.3 1.2a11.4 11.4 0 0 1 6 0c2.3-1.5 3.3-1.2 3.3-1.2.6 1.7.2 2.9.1 3.2.8.8 1.2 1.9 1.2 3.2 0 4.6-2.8 5.6-5.5 5.9.4.4.8 1.1.8 2.2v3.3c0 .3.2.7.8.6A12 12 0 0 0 12 .3Z" /></svg>
      </span>
      <span class="index">01</span>
      <span class="name">GitHub</span>
      <span class="value">@oppenheimor</span>
      <span class="arrow" aria-hidden="true">→</span>
    </a>

    <a class="social" href="https://oppenheimor.github.io">
      <span class="icon">
        <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9" fill="none" stroke="currentColor" stroke-width="1.8"/><path d="M3 12h18M12 3c3 3.2 4.5 6.2 4.5 9S15 17.8 12 21c-3-3.2-4.5-6.2-4.5-9S9 6.2 12 3Z" fill="none" stroke="currentColor" stroke-width="1.8"/></svg>
      </span>
      <span class="index">02</span>
      <span class="name">Website</span>
      <span class="value">oppenheimor.github.io</span>
      <span class="arrow" aria-hidden="true">→</span>
    </a>
  </div>
</section>

<style>
  .elsewhere {
    position: relative;
    padding: 10vh 0 12vh;
  }

  .sectionNumber {
    position: absolute;
    top: 5vh;
    right: -0.05em;
    z-index: -1;
    color: transparent;
    font-size: clamp(120px, 22vw, 220px);
    font-weight: 800;
    line-height: 1;
    letter-spacing: -0.04em;
    -webkit-text-stroke: 1px oklch(0.2 0.006 75 / 0.1);
    user-select: none;
  }

  .wordmark {
    position: absolute;
    right: -0.06em;
    bottom: -0.18em;
    z-index: -1;
    color: transparent;
    font-size: clamp(170px, 32vw, 320px);
    font-weight: 800;
    line-height: 0.78;
    letter-spacing: -0.06em;
    -webkit-text-stroke: 1px oklch(0.2 0.006 75 / 0.08);
    user-select: none;
  }

  .label {
    display: flex;
    width: max-content;
    max-width: 100%;
    align-items: center;
    gap: 16px;
    margin: 0 0 48px;
    color: var(--text-muted);
    font-size: 14px;
    font-weight: 400;
    letter-spacing: 0.4em;
    text-transform: uppercase;
  }

  .label::before {
    width: 28px;
    height: 1px;
    background: var(--ink);
    content: "";
  }

  .socials { border-top: 1px solid var(--hairline); }

  .social {
    display: grid;
    min-width: 0;
    grid-template-columns: 36px 28px minmax(120px, 0.45fr) minmax(0, 1fr) 20px;
    align-items: center;
    gap: 16px;
    padding: 26px 4px;
    border-bottom: 1px solid var(--hairline);
    text-decoration: none;
    transition: padding-left 0.7s var(--ease-out);
  }

  .icon {
    display: inline-flex;
    width: 36px;
    height: 36px;
    flex: none;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--hairline);
    border-radius: 11px;
    transition: background 0.5s var(--ease-out), border-color 0.5s var(--ease-out), transform 0.6s var(--ease-out);
  }

  svg {
    display: block;
    width: 17px;
    height: 17px;
    fill: currentColor;
  }

  .index,
  .value {
    font-family: ui-monospace, "SF Mono", Menlo, monospace;
  }

  .index {
    color: var(--text-muted);
    font-size: 12px;
    letter-spacing: 0.08em;
  }

  .name {
    background-image: linear-gradient(var(--highlight), var(--highlight));
    background-position: 0 68%;
    background-repeat: no-repeat;
    background-size: 0 42%;
    font-size: clamp(22px, 3.2vw, 28px);
    font-weight: 800;
    letter-spacing: -0.02em;
    transition: background-size 0.6s var(--ease-out);
  }

  .value {
    min-width: 0;
    overflow: hidden;
    color: var(--text-muted);
    font-size: 13px;
    text-overflow: ellipsis;
    white-space: nowrap;
    transition: color 0.5s var(--ease-out);
  }

  .arrow {
    color: var(--ink);
    font-size: 20px;
    opacity: 0;
    transform: translateX(-8px);
    transition: opacity 0.5s var(--ease-out), transform 0.7s var(--ease-out);
  }

  .social:focus-visible .icon,
  .social:focus-visible .name {
    background-color: var(--highlight);
    border-color: var(--highlight);
  }

  @media (hover: hover) and (pointer: fine) {
    .social:hover { padding-left: 22px; }
    .social:hover .icon { border-color: var(--highlight); background: var(--highlight); transform: rotate(-6deg) scale(1.07); }
    .social:hover .name { background-size: 100% 42%; }
    .social:hover .value { color: var(--ink); }
    .social:hover .arrow { opacity: 1; transform: translateX(0); }
  }

  @media (max-width: 700px) {
    .elsewhere { padding: 8vh 0; }

    .wordmark { font-size: clamp(150px, 58vw, 230px); }

    .label {
      margin-bottom: 38px;
      font-size: 12px;
      letter-spacing: 0.3em;
    }

    .social {
      grid-template-columns: 32px minmax(max-content, 0.45fr) minmax(0, 1fr) 18px;
      gap: 10px;
      padding: 18px 0;
    }

    .icon { width: 32px; height: 32px; border-radius: 10px; }
    .index { display: none; }
    .name { white-space: nowrap; font-size: 18px; }
    .value { font-size: 10.5px; }
    .arrow { opacity: 1; font-size: 17px; transform: none; }
  }
</style>
