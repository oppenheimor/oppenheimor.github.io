<section class="hero" aria-labelledby="hero-title">
  <h1 id="hero-title">
    <span class="lineMask"><span>创造力</span></span>
    <span class="lineMask"><span>来源于</span></span>
    <span class="lineMask"><span>热爱生活</span></span>
    <span class="illustration" aria-hidden="true">
      <svg viewBox="0 0 240 250" role="presentation">
        <ellipse class="orbit" cx="120" cy="145" rx="91" ry="35" transform="rotate(-18 120 145)" />
        <circle class="seed" cx="120" cy="145" r="34" />
        <path class="stem" d="M120 119c-2-38 15-67 48-84" />
        <path class="leaf" d="M150 59c8-29 34-39 55-32-3 25-22 43-55 32Z" />
        <path class="leaf leafLeft" d="M127 83c-23-24-48-21-64-7 13 23 37 30 64 7Z" />
        <circle class="satellite" cx="36" cy="166" r="7" />
      </svg>
    </span>
  </h1>
  <div class="sub">
    <span class="zh">沈佳棋</span>
    <span class="en">全栈工程师 / AI Builder</span>
  </div>
  <a class="scrollHint" href="#garden">↓ 往下滑</a>
</section>

<style>
  .hero {
    position: relative;
    display: flex;
    min-height: 86vh;
    flex-direction: column;
    justify-content: center;
  }

  h1 {
    position: relative;
    margin: 0;
    font-size: clamp(64px, 7vw, 104px);
    font-weight: 700;
    line-height: 1.06;
    letter-spacing: -0.025em;
  }

  .lineMask {
    display: block;
    padding-bottom: 0.04em;
    overflow: hidden;
  }

  .lineMask > span {
    display: block;
    animation: lift 1.5s var(--ease-out) forwards;
    transform: translateY(112%);
  }

  .lineMask:nth-child(1) > span { animation-delay: 0.1s; }
  .lineMask:nth-child(2) > span { animation-delay: 0.24s; }
  .lineMask:nth-child(3) > span { animation-delay: 0.38s; }

  .illustration {
    position: absolute;
    right: 24px;
    bottom: 0.12em;
    z-index: 1;
    display: block;
    width: min(24vw, 180px);
    opacity: 0;
    animation: illustrationIn 1.5s var(--ease-out) 0.5s forwards;
    pointer-events: none;
  }

  svg {
    display: block;
    width: 100%;
    overflow: visible;
    filter: drop-shadow(0 16px 24px oklch(0.04 0.025 255 / 0.72));
  }

  .orbit {
    fill: none;
    stroke: var(--ink);
    stroke-width: 2;
    stroke-dasharray: 3 8;
  }

  .seed {
    fill: var(--highlight);
    stroke: var(--ink);
    stroke-width: 2;
  }

  .stem,
  .leaf {
    fill: none;
    stroke: var(--ink);
    stroke-width: 5;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .leaf {
    fill: var(--highlight-soft);
  }

  .leafLeft { fill: var(--signal-soft); }

  .satellite {
    fill: var(--signal);
    filter: drop-shadow(0 0 7px oklch(0.79 0.105 205 / 0.38));
    animation: satellite 5.5s ease-in-out infinite;
    transform-origin: 120px 145px;
  }

  .sub {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 20px;
    margin-top: 40px;
    opacity: 0;
    animation: fadeUp 1.4s var(--ease-out) 1s forwards;
    transform: translateY(18px);
  }

  .zh {
    font-size: 20px;
    font-weight: 500;
    letter-spacing: 0.3em;
  }

  .en,
  .scrollHint {
    color: var(--text-muted);
    font-size: 14px;
  }

  .en { letter-spacing: 0.14em; }

  .scrollHint {
    position: absolute;
    bottom: 5vh;
    left: 0;
    opacity: 0;
    letter-spacing: 0.3em;
    text-decoration: none;
    animation: fadeUp 1.4s var(--ease-out) 1.8s forwards;
    transform: translateY(18px);
  }

  @keyframes lift { to { transform: translateY(0); } }
  @keyframes fadeUp { to { opacity: 1; transform: translateY(0); } }
  @keyframes illustrationIn { to { opacity: 1; transform: translateY(-5%); } }
  @keyframes satellite { 50% { transform: rotate(18deg) translateX(8px); } }

  @media (max-height: 500px) and (min-width: 701px) {
    .hero { min-height: 88svh; }
    h1 { font-size: clamp(54px, 15vh, 68px); }
    .illustration { width: 120px; }
    .sub { margin-top: 20px; }
    .scrollHint { display: none; }
  }

  @media (max-width: 700px) {
    .hero { min-height: 74svh; }

    h1 {
      font-size: clamp(52px, 15.5vw, 68px);
      line-height: 1.08;
    }

    .illustration {
      right: 0;
      bottom: -0.62em;
      width: min(24vw, 88px);
    }

    .sub {
      gap: 12px;
      margin-top: 58px;
    }

    .zh { font-size: 16px; }

    .en {
      font-size: 10px;
      line-height: 1.6;
      letter-spacing: 0.09em;
    }

    .scrollHint {
      bottom: 3vh;
      font-size: 11px;
    }
  }
</style>
