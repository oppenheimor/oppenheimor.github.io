<section class="hero" aria-labelledby="hero-title">
  <h1 id="hero-title">
    <span class="lineMask"><span>创造力</span></span>
    <span class="lineMask"><span>来源于</span></span>
    <span class="lineMask"><span>热爱生活</span></span>
  </h1>
  <div class="sub">
    <span class="zh">沈佳棋</span>
    <span class="en">全栈工程师 / AI Builder</span>
  </div>
  <a class="scrollHint" href="#garden">↓ 往下滑</a>
</section>

<style>
  .hero {
    position: relative;
    isolation: isolate;
    display: flex;
    min-height: 100svh;
    flex-direction: column;
    justify-content: center;
  }

  .hero::before {
    position: absolute;
    inset: 0 auto 0 50%;
    z-index: 0;
    width: 100vw;
    background-color: var(--canvas);
    background-image:
      linear-gradient(90deg, oklch(0.16 0.006 90 / 0.82) 0%, oklch(0.15 0.006 90 / 0.68) 28%, oklch(0.14 0.006 90 / 0.38) 50%, transparent 78%),
      linear-gradient(180deg, color-mix(in oklch, var(--canvas) 28%, transparent), transparent 30%, var(--canvas) 100%),
      url('/images/home-hero-cat.webp');
    background-position: center, center, center right;
    background-size: cover, cover, cover;
    background-repeat: no-repeat;
    content: '';
    pointer-events: none;
    transform: translateX(-50%);
  }

  .hero::after {
    position: absolute;
    right: auto;
    bottom: 0;
    left: 50%;
    z-index: 0;
    width: 100vw;
    height: 20%;
    background: linear-gradient(to bottom, transparent, var(--canvas) 88%);
    content: '';
    pointer-events: none;
    transform: translateX(-50%);
  }

  .hero > * { position: relative; z-index: 1; }

  h1 {
    position: relative;
    z-index: 1;
    margin: 0;
    font-family: "YeZiGongChangXiaoShiTou", "PingFang SC", "Noto Sans SC", sans-serif;
    font-size: clamp(64px, 7vw, 104px);
    font-weight: 400;
    line-height: 1.06;
    letter-spacing: -0.025em;
  }

  .lineMask {
    display: block;
    padding-bottom: 0.04em;
    overflow: hidden;
  }

  .lineMask > span {
    display: block;
    animation: lift 1.5s var(--ease-out) forwards;
    transform: translateY(112%);
  }

  .lineMask:nth-child(1) > span { animation-delay: 0.1s; }
  .lineMask:nth-child(2) > span { animation-delay: 0.24s; }
  .lineMask:nth-child(3) > span { animation-delay: 0.38s; }

  .sub {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 20px;
    margin-top: 40px;
    opacity: 0;
    animation: fadeUp 1.4s var(--ease-out) 1s forwards;
    transform: translateY(18px);
  }

  .zh {
    font-size: 24px;
    font-weight: 500;
    letter-spacing: 0.3em;
  }

  .en,
  .scrollHint {
    color: var(--text-muted);
    font-size: 16px;
  }

  .en { letter-spacing: 0.14em; }

  .scrollHint {
    display: block;
    width: max-content;
    margin-top: 96px;
    opacity: 0;
    letter-spacing: 0.3em;
    text-decoration: none;
    animation: fadeUp 1.4s var(--ease-out) 1.8s forwards;
    transform: translateY(18px);
  }

  @keyframes lift { to { transform: translateY(0); } }
  @keyframes fadeUp { to { opacity: 1; transform: translateY(0); } }

  @media (max-height: 500px) and (min-width: 701px) {
    .hero { min-height: 100svh; }
    h1 { font-size: clamp(54px, 15vh, 68px); }
    .sub { margin-top: 20px; }
    .scrollHint { display: none; }
  }

  @media (max-width: 900px) {
    .hero {
      min-height: 100svh;
      justify-content: flex-start;
      padding-top: clamp(96px, 14svh, 128px);
    }

    .hero::before {
      background-image:
        linear-gradient(90deg, oklch(0.16 0.006 90 / 0.88) 0%, oklch(0.15 0.006 90 / 0.74) 34%, oklch(0.14 0.006 90 / 0.42) 62%, transparent 100%),
        linear-gradient(180deg, color-mix(in oklch, var(--canvas) 24%, transparent), transparent 42%, var(--canvas) 100%),
        url('/images/home-hero-cat-mobile.webp');
      background-position: center, center, 86% top;
      background-size: cover, cover, auto 112%;
    }

    h1 {
      font-size: clamp(52px, 15.5vw, 68px);
      line-height: 1.08;
    }

    .sub {
      gap: 12px;
      margin-top: 18px;
    }

    .zh { font-size: 18px; }

    .en {
      color: var(--text-secondary);
      font-size: 12px;
      line-height: 1.6;
      letter-spacing: 0.09em;
    }

    .scrollHint {
      margin-top: 78px;
      color: var(--text-secondary);
      font-size: 11px;
    }
  }
</style>
